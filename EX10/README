Extend the JDBC application code to perform CRUD operations through JavaFX UI.

Create a JavaFX menu driven application to perform: Insert, Select, Update and Delete queries on MenuItem and Restaurant tables.
